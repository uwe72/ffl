# Lokale Entwicklung: Datei nach .env kopieren (.env ist gitignored).
# Das Secret muss mindestens 32 Byte Schlüsselmaterial für HS256 liefern.
# In Docker/Prod wird APP_JWT_SECRET über das Compose File gesetzt und hat Vorrang vor dieser Datei.
APP_JWT_SECRET=
